-- ============================================================
-- gbrain — наполнение: ещё два агента роя (до четвёрки).
--   • Стратег   🟡 — планировщик: разбивает цели на подзадачи
--   • Исследователь 🟢 — собирает информацию и факты по задаче
-- Идемпотентно: повторный запуск не плодит дубли.
-- ============================================================

insert into public.agents (name, color, tg_bot, role, status)
values
  ('Стратег', '#facc15', null,
   'Планировщик роя: разбивает большую цель на конкретные подзадачи для других агентов', 'idle'),
  ('Исследователь', '#4ade80', null,
   'Исследователь роя: собирает информацию, факты и источники по задаче', 'idle')
on conflict (name) do nothing;
