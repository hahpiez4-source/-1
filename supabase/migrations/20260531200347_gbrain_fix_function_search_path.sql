-- ============================================================
-- Полировка безопасности: фиксируем search_path у функции-триггера.
--
-- Зачем: без заданного search_path функция ищет объекты по «изменяемому»
-- пути. Теоретически кто-то мог бы подсунуть свою таблицу/функцию.
-- Лечение: жёстко задаём пустой search_path. Функция использует только
-- now() (она в pg_catalog и доступна всегда), поэтому ничего не ломается.
-- ============================================================

alter function public.set_updated_at() set search_path = '';
