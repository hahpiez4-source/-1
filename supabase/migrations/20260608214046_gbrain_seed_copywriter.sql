-- ============================================================
-- gbrain — новый агент роя: «Копирайтер» ✍️
--   Мастер слова: пишет и редактирует тексты (посты, объявления,
--   письма, описания, заголовки, слоганы). Обычный «мыслитель»
--   (handler think), без внешних ключей.
-- Идемпотентно: повторный запуск не плодит дубли.
-- ============================================================

insert into public.agents (name, color, tg_bot, role, status)
values
  ('Копирайтер', '#a78bfa', null,
   'Копирайтер роя: пишет и редактирует тексты — посты, объявления, письма, описания, заголовки, слоганы',
   'idle')
on conflict (name) do nothing;
