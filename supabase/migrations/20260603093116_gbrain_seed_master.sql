-- ============================================================
-- gbrain — новый агент роя: «Мастер» 🛠️
--   Руки роя: ищет в интернете и запускает код (через groq/compound).
-- Идемпотентно: повторный запуск не плодит дубли.
-- ============================================================

insert into public.agents (name, color, tg_bot, role, status)
values
  ('Мастер', '#fb923c', null,
   'Мастер-исполнитель роя с реальными инструментами: ищет в интернете и запускает код для точных расчётов, обработки данных и проверки фактов',
   'idle')
on conflict (name) do nothing;
